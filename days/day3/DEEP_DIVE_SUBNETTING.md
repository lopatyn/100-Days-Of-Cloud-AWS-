# Deep Dive: Магія CIDR та підмереж 🧠🔢

Багато хто лякається чисел типу `173.31.96.0/20`. Давай розберемося, як зрозуміти ці "дроби" без калькулятора.

## 1. Що таке CIDR?
**CIDR (Classless Inter-Domain Routing)** — це спосіб запису маски мережі. Число після косої риски (наприклад, `/20`) вказує, скільки біт з 32-х бітної IP-адреси зарезервовано під "назву мережі" (Network ID).

- Решта біт ($32 - 20 = 12$) залишається для хостів (пристроїв).

## 2. Як розрахувати кількість адрес?
Формула проста: $2^{(32 - mask)}$.
- Для `/20`: $2^{12} = 4096$ адрес.
- Для `/24`: $2^{(32-24)} = 2^8 = 256$ адрес.

## 3. Чому саме 173.31.96.0/20? (Твій випадок)
Твоя VPC має маску `/16` (перші два опадки `173.31` фіксовані).
Ти ріжеш її на підмережі `/20`.

**Математика в третьому октеті:**
- `/16` — 2 октети мережі.
- `/24` — 3 октети мережі.
- `/20` знаходиться рівно посередині третього октету.
- Крок (increment) для `/20` розраховується як $256 / 2^{(20-16)} = 256 / 2^4 = 256 / 16 = 16$.

Отже, твої підмережі йдуть з кроком **16** у третьому октеті:
1. `173.31.0.0/20`
2. `173.31.16.0/20`
3. `173.31.32.0/20`
4. `173.31.48.0/20`
5. `173.31.64.0/20`
6. `173.31.80.0/20`
7. **`173.31.96.0/20`** — Твоя нова підмережа! ✅
8. `173.31.112.0/20` ... і так далі.

## 4. Підказка для "Senior" погляду 💡
Коли ти бачиш список: `0, 16, 32, 48, 64, 80`, ти відразу розумієш, що це таблиця множення на 16. Наступне вільне число — **96**. Саме тому AWS запропонував або ти обрав 96.

## 5. Важливо: Резервні адреси AWS ⚠️
В кожній підмережі AWS забирає 5 адрес:
- `.0`: Network address.
- `.1`: VPC Router.
- `.2`: DNS Server.
- `.3`: Future use.
- `.255`: Network Broadcast (хоча AWS не підтримує справжній broadcast, він все одно зарезервований).

Тепер ти знаєш, що мережі — це не магія, а просто двійкова математика! 🚀🦾
