# День 3: Створення підмережі (Subnet) 🌐

> **Hook:** Скільки "квартир" можна створити в одному ІТ-будинку, щоб вони не заважали одна одній? 🏢🏗️
>
> **Problem-Solution-Value:** Велика мережа (VPC) без підмереж — це хаос. Ми виділили сегмент `nautilus-subnet` (173.31.96.0/20), щоб ізолювати ресурси та підготувати грунт для майбутньої міграції серверів. Це забезпечує структурованість, безпеку на рівні мережевої логіки та підготовку до High Availability (HA).
>
> **Call to Action:** Розділяй та володарюй! Навчись сегментувати хмару правильно. Гайд по мережах AWS нижче! 👇

## Бізнес та інфраструктурний контекст

### Чому це важливо:
Сегментація мережі — це перший крок до **Security Isolation** та **Scalability**. Для бізнесу це означає можливість розгортати різні рівні програми (Web, App, DB) у різних підмережах з власними фаєрволами (NACL). Це критично для міграції інфраструктури Nautilus DevOps, оскільки дозволяє поступово переносити сервіси без ризику для всієї мережі.

### Оптимізація витрат (FinOps):
- **Free Tier:** Створення підмереж в AWS є безкоштовним. Ви платите за ресурси всередині них (EC2, NAT Gateway) та за передачу трафіку між AZ.
- **Оптимізація:** Правильне планування підмереж у різних Availability Zones дозволяє уникнути витрат на складну перемаршрутизацію в майбутньому.

### Операційна область:
- **Середовище:** AWS Cloud.
- **Рівень:** Network Layer (Мережевий рівень).
- **Етап:** Foundation & Infrastructure setup.

### Ризики:
Якщо ігнорувати правильне розбиття на підмережі (CIDR), можна швидко "витратити" всі доступні IP-адреси або створити перетини (Overlapping), що унеможливить VPN або Peering з іншими мережами компанії.

## Розбір лабораторної роботи: Побудова VPC Subnets

### Архітектура хмари (Доступ до даних)
Схема показує логічний поділ VPC `173.31.0.0/16` на сегменти `/20`.

![AWS Architecture Subnet Day 3](subnet_architecture.png)

### Покрокове впровадження

### Крок 1: Створення підмережі
Використовуємо AWS CLI або Console для виділення адресної площі.
```bash
# Створення підмережі nautilus-subnet у вибраній VPC
aws ec2 create-subnet \
    --vpc-id vpc-xxxxxxxx \
    --cidr-block 173.31.96.0/20 \
    --tag-specifications 'ResourceType=subnet,Tags=[{Key=Name,Value=nautilus-subnet}]'
```
**Очікуваний результат:** JSON з ID нової підмережі та статусом "pending/available".

### Крок 2: Перевірка діапазону (CIDR Validation)
```bash
# Перевірка всіх підмереж у VPC
aws ec2 describe-subnets --filters "Name=vpc-id,Values=vpc-xxxxxxxx"
```
**Очікуваний результат:** Список підмереж, включаючи нову `173.31.96.0/20` без перетинів з існуючими (0, 16, 32, 48...).

## DevOps контекст та альтернативи
- **Infrastructure as Code:** В реальному середовищі ми ніколи не створюємо підмережі вручну. Ми використовуємо Terraform модуль `vpc`, де просто вказуємо список `public_subnets` та `private_subnets`.
- **Kubernetes:** В EKS підмережі мають бути спеціально теговані (`kubernetes.io/cluster/<name>`), щоб Cloud Controller міг створювати Load Balancers.

## Пастки для джунів (Поширені помилки ⚠️)
- **Зарезервовані IP:** AWS забирає **5 IP-адрес** у кожній підмережі. Якщо ти створиш маленьку підмережу `/30` (4 IP), ти не зможеш запустити в ній жодного сервера.
- **Overlapping CIDR:** Спроба створити підмережу, яка вже "захоплена" іншою. Завжди тримай таблицю розбиття (як у нашому Deep Dive).
- **Public vs Private:** Створення підмережі не робить її публічною автоматично — потрібен Internet Gateway та запис у Route Table.

## Резюме для співбесіди (Best Practices)
1. **AZ Spread:** Завжди розподіляйте підмережі між мінімум 2-3 Availability Zones для High Availability.
2. **CIDR Sizing:** Обирайте розмір підмережі "на виріст". `/20` (4096 IP) — хороший вибір для великих VPC.
3. **Naming Convention:** Використовуйте зрозумілі теги `Name` (наприклад, `prod-public-1a`).
4. **Reserved IPs:** Пам'ятайте про 5 зарезервованих адрес (Gateway, DNS, Broadcast, Future use).
5. **White Hat Insight:** Підмережі є фізичними межами для мережевих ACL (NACL). Це твій другий ешелон захисту після Security Groups. [Детальний розрахунок CIDR у Deep Dive](./DEEP_DIVE_SUBNETTING.md).

---
**Попередній день:** [День 2: Створення Security Group](../day2)
**Наступний крок:** [День 4: Запуск EC2 Інстансу]
